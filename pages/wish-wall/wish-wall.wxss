/* pages/wish-wall/wish-wall.wxss */

page {
  background-color: #ffffff;
}

.wish-wall-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background-color: #ffffff;
}

/* Header */
.header {
  background-color: white;
  border-bottom: 1rpx solid #eef2f7;
  padding: 16rpx;
  padding-top: 32rpx;
  box-shadow: 0 1rpx 2rpx rgba(15, 23, 42, 0.03);
  z-index: 10;
  position: sticky;
  top: 0;
}

.header-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.header-title-section {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.heart-icon {
  font-size: 32rpx;
}

.header-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1f2937;
}

.header-action {
  display: flex;
  align-items: center;
  gap: 8rpx;
  background-color: #10b981;
  color: white;
  padding: 10rpx 16rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 600;
  box-shadow: 0 2rpx 6rpx rgba(16, 185, 129, 0.2);
}

.publish-icon {
  font-size: 24rpx;
  font-weight: bold;
}

.publish-text {
  font-size: 24rpx;
}

/* Sort Container */
.sort-container {
  display: flex;
  gap: 12rpx;
}

.sort-btn {
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  background-color: #f8fafc;
  color: #475569;
  font-size: 24rpx;
  font-weight: 500;
  transition: all 0.2s;
  border: 1rpx solid #eef2f7;
}

.sort-btn.active {
  background-color: #10b981;
  color: white;
  box-shadow: 0 2rpx 8rpx rgba(16, 185, 129, 0.3);
}

/* Wishes Scroll */
.wishes-scroll {
  flex: 1;
  overflow-y: auto;
}

/* Empty State */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 16rpx;
  text-align: center;
}

.empty-icon {
  font-size: 64rpx;
  margin-bottom: 16rpx;
}

.empty-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #1f2937;
  margin-bottom: 8rpx;
}

.empty-text {
  font-size: 24rpx;
  color: #64748b;
}

/* Wishes List */
.wishes-list {
  padding: 16rpx 12rpx;
}

.wish-card {
  background-color: white;
  border: 1rpx solid #eef2f7;
  border-radius: 12rpx;
  padding: 20rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 1rpx 3rpx rgba(15, 23, 42, 0.05);
  transition: all 0.2s;
}

.wish-card:active {
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
  transform: scale(0.98);
}

/* Card Header */
.card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.title-section {
  flex: 1;
}

.wish-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #1f2937;
  display: block;
  margin-bottom: 12rpx;
  line-height: 1.4;
}

.tags-row {
  display: flex;
  gap: 8rpx;
  flex-wrap: wrap;
}

.category-tag {
  display: inline-block;
  padding: 8rpx 16rpx;
  background-color: #d1fae5;
  color: #065f46;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 700;
}

.verified-tag {
  display: inline-block;
  padding: 8rpx 16rpx;
  background-color: #dbeafe;
  color: #0c4a6e;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 700;
}

/* Description */
.wish-description {
  font-size: 24rpx;
  color: #475569;
  display: block;
  margin-bottom: 16rpx;
  line-height: 1.6;
}

/* Budget and Meta */
.budget-meta-section {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 12rpx;
  padding-bottom: 12rpx;
  border-bottom: 1rpx solid #eef2f7;
}

.budget-info {
  flex: 1;
}

.budget-text {
  font-size: 26rpx;
  font-weight: 700;
  color: #f59e0b;
  display: block;
  margin-bottom: 10rpx;
}

.meta-info {
  display: flex;
  gap: 16rpx;
}

.meta-item {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
}

/* User and Action */
.user-action-section {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.user-info {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.user-avatar {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #ec4899 0%, #a855f7 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: 600;
  flex-shrink: 0;
}

.user-details {
  display: flex;
  flex-direction: column;
}

.user-name {
  font-size: 24rpx;
  font-weight: 600;
  color: #1f2937;
  display: block;
}

.user-status {
  font-size: 24rpx;
  color: #64748b;
  display: block;
  margin-top: 2rpx;
}

.contact-btn {
  display: flex;
  align-items: center;
  gap: 8rpx;
  background-color: #10b981;
  color: white;
  padding: 12rpx 16rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 700;
  box-shadow: 0 2rpx 6rpx rgba(16, 185, 129, 0.18);
}

.contact-icon {
  font-size: 24rpx;
}

.contact-text {
  font-size: 24rpx;
}

/* Tips Container */
.tips-container {
  background-color: white;
  border-top: 1rpx solid #eef2f7;
  padding: 16rpx;
  margin-top: 12rpx;
}

.tips-content {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  background-color: #eff6ff;
  border-left: 4rpx solid #0ea5e9;
  padding: 12rpx 16rpx;
  border-radius: 4rpx;
}

.tips-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #0369a1;
  display: block;
}

.tips-item {
  font-size: 22rpx;
  color: #0369a1;
  display: block;
  line-height: 1.5;
}

/* Footer Spacing */
.footer-spacing {
  height: 40rpx;
}
